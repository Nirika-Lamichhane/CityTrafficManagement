# notificationpy.py (only handles displaying local notification)
from plyer import notification

def display_local_notification(title, message):
    notification.notify(
        title=title,
        message=message,
        timeout=10
    )
